" Config

" enables fzf for fuzzy search
" requires installation of fzf, ripgrep, universal-ctags, and fd
let enable_fzf = 1

" enables git integration
" required git (which you should have anyway for plug)
let enable_git = 1
